name: 'add test'

on:
  push:
    branches:
      [ dev ]
jobs:

 build:
 
   runs-on: ubuntu-latest
   
   steps:
   - uses: actions/checkout@v3
   - run: echo hello
   - run: gcc -Wall he.c -o he
   - run: ./he
 
